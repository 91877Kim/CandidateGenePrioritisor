# prioritising_input.txt
# Lines starting with # are ignored. Order of non-comment lines must match below.
#
# Line 1: Homozygous SNP VCF path (for mapping). Leave empty to skip mapping.
# Line 2: Hawaiian allele-ratio VCF path (for mapping). Leave empty to skip mapping.
# Line 3: Optional mapping table path (e.g. outputs/mapping_interval_X_0_variants.csv).
#         If non-empty and file exists: use this table and run multi_agent_with_mapping only.
#         If all of Lines 1–3 are empty: run prioritisation without mapping.
#         Otherwise: run mapping on the two VCFs above, then run multi_agent_with_mapping with the generated table.
# Line 4: Main variant CSV path (e.g. drp1.csv).
# Line 5: Optional user annotation CSV filename in script directory (e.g. user_annotation.csv). Leave empty to skip.
# Line 6: Agent1 prompt (literature evaluation; phenotype + mechanisms).
# Line 7: Agent2 prompt (reasoning / synthesis for the gene set).
# Line 8: How many LLM runs on this gene set (integer, e.g. 1 or 3).



drp1_nonsubstracted_drp1format.csv
user_file.csv
You are a C. elegans literature evaluator. Score the provided publication summary for its mechanistic relevance to a specific mutant phenotype. The Phenotype Context: MCM neurons — short for Mystery Cells of the Male. They are a pair of male-specific interneurons in the head, one left and one right. the mutated phenotype we are studying is the loss of MCM neurons in male C. elegans arising from the AMso(the one which normally gives MCM) divides symmetrically to generate two glial cells instead of the MCM. Target Mechanisms/Keywords: asymmetric division, neuronal cell fate, glial-to-neuronal fate transition. Rules: Look for mechanisms that biologically explain the phenotype. Do not just look for superficial word matches; look for mechanistic capability. Score broad, pleiotropic housekeeping genes low unless they are explicitly linked to the target mechanisms. Extract a 1-sentence mechanistic summary.
I am a C. elegans geneticist studying developmental biology. I have performed a forward genetic screen to identify mutants that are missing the male specific MCM neurons. MCM neurons are normally derived from the division of amphid socket(AMso) glial cells during larval stage of sexual maturation. drp1(the causal gene we are finding) is a Class III mutation - The AMso divides symmetrically to generate two glial cells instead of the MCM.
5